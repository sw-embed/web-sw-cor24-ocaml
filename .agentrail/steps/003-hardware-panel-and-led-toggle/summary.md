Added floating hardware panel (bottom-right, S2 clickable + D2 LED indicator) ported from web-sw-cor24-apl with OCaml-themed violet accent colors. Session gains set_switch/led_on wrappers around cor24-emulator; App pushes S2 state before each batch and samples LED after. led-toggle replaced with a one-shot read (the CLI's infinite loop overflows the interp call stack in 6 iterations) so toggling S2 and hitting Run produces 0/1 output matching the LED state. Added LOCAL_OVERRIDE allowlist in sync-demos.sh so future syncs preserve the hand-edited file. cargo test (6+2) + trunk build + markdown-checker all green. Saga done.