Two post-deploy fixes shipped. (1) text-adventure prompt: tightened PC_POLL_WINDOW from 64 to 16 in lib.rs Tick handler so parser warm-up (PC range ~30 bytes in PVM helpers) no longer false-positives as 'awaiting input'; the actual read_line UART RX poll is a few-byte fetch+branch easily within 16. New integration test text_adventure_seed_prints_cave_description_before_input_prompt mirrors the App's PC stability loop and asserts the cave description is in cleaned output before stability triggers (settled after 138M instrs at PC=0xced, samples within 9 bytes). All 4 interactive demos (echo-loop, repl-session, text-adventure, guess) still reach awaiting_input correctly. (2) New hello-world demo: print_endline "Hello, World!", slotted alphabetically between hello and higher-order-lists. docs/demos.md updated. 13 integration + 12 lib tests pass. Pages rebuilt + pushed.