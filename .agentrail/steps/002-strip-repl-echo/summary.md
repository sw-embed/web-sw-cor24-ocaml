Added strip_prompt_echoes() helper that scans raw UART for line-leading '> ' and elides through the next newline. clean_output() now strips REPL prompt + echoed source + trailing CR. Session::new() pads source with '\n' before EOT so one-shot programs without trailing newline still get a clean line break (otherwise echo and result fuse into '> print_int 4242'). print_int_42 test strengthened to assert exact 'cleaned == "42"'. Two new unit tests cover the helper. cargo test (3) + trunk build clean.