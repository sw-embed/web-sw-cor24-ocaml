Added tests/demos.rs integration test that runs every demo end-to-end. Found and fixed two real bugs: led-blink and led-toggle had multi-line semicolon-sequenced sources that the line-by-line OCaml REPL parsed as 5x PARSE ERROR. scripts/sync-demos.sh now has a COLLAPSE_NEWLINES allowlist that flattens those two on copy. led-blink now outputs '1\n0\n1', led-toggle outputs 'EVAL ERROR' (OCaml interp call stack overflows from non-tail-recursive loop). Docs/demos.md updated. cargo test (5 unit + 2 integration) all pass; trunk build + markdown-checker clean. Saga done.