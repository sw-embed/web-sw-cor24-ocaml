Wrote scripts/serve.sh (port 9735, mkdir lock at target/.trunk-dist.lock with stale-pid recovery + EXIT trap cleanup) and scripts/build-pages.sh (release build with /web-sw-cor24-ocaml/ public-url, same lock, rsync to pages/ preserving .nojekyll). Verified: serve.sh -> HTTP 200 on 9735; second instance refuses; build-pages.sh produces pages/{index.html,*.css,*.wasm,*.js,favicon,.nojekyll}; lock auto-cleans. pages/.nojekyll committed; rest of pages/ gitignored.