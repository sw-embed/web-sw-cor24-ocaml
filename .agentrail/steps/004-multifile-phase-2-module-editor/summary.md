Phase 2 of multi-file demos plan shipped. New src/components/{mod.rs,module_editor.rs} exposes ModuleEditor (function component) + ModuleFile, patterned on web-sw-cor24-plsw MacroEditor and trimmed to Phase 2 scope (per-cell edit + collapse, no add/remove/upload). Factored concat_with_aux as free function in src/demos.rs so both baked-in (Demo::concat_main) and live-edited paths share directive injection. App gained aux_edits Vec ModuleFile state, hydrated from auxiliary_files on demo selection / Component::create. New Msg variants AuxFileChanged + AuxFileToggleCollapse. start_run builds source via concat_with_aux on aux_edits (not baked-in aux), so edits to either pane flow into next Run. CSS in src/ui.css adds /Auxiliary module files/ block. New regression test concat_with_aux_edited_runs_through_session verifies edited math.ml (multiply not add) produces 6 not 5. Docs updated: docs/demos.md modules-multifile entry notes Phase 2 shipped; docs/multiple-file-demos-plan.md Phase 2 section moved to SHIPPED with concrete refs, next-saga line now points at Phase 3 (add/remove/upload/persistence). 12 lib + 11 fast integration tests pass; pages rebuilt + pushed.