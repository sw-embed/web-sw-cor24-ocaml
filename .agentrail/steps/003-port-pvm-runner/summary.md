Ported pv24t into src/runner.rs adapted for .p24m multi-unit images. Inline P24M header parser, memory layout at LOAD_ADDR=0x010000, code-section-relative Jmp/Call targets + code_abs_base translation, XCall via per-unit IRT with same translation, XLoadg/XStoreg via unit table, UART source+EOT seed + GETC pause for interactive mode. Added src/config.rs for OUT_DIR .p24m include. cargo test passes: print_int 42 -> 6410 instrs, halts, '42' in cleaned output.