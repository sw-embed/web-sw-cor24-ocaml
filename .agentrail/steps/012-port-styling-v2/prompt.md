Port the visual design from ../web-sw-cor24-basic/src/ui.css. Same body as the original step 006 prompt at .agentrail/steps/006-port-styling/prompt.md: copy ui.css verbatim, add a .demo-description rule for the new caption (small font, muted color, single line ellipsis), pick a distinguishing accent color from BASIC's #58a6ff (e.g. teal/purple/violet — the favicon already uses #8945ee, so reuse that hex for cohesion). Favicon is already correct (favicon -T -t OC -r 39 -f 8945ee from step 001). Validation: trunk build clean, cargo test still passes.