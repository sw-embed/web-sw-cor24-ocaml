text-adventure take bug fixed. Root cause: pickup was a pure function of room with no 'already taken' tracking, and describe Cave unconditionally mentioned the lamp. Extended loop state to (r, (inv, (lt, kt))) -- nested 2-tuples so fst/snd accessors handle it without 4-tuple destructure quirks. Thread lt/kt through describe and pickup so they branch on what's been collected. Take handler sets the appropriate flag via inner match on . Hand-edited examples/text-adventure.ml under LOCAL_OVERRIDE; added rationale comment in scripts/sync-demos.sh. CLI source ../sw-cor24-ocaml/tests/demo_adventure.ml has the same bug -- separate CLI follow-up. Regression test text_adventure_take_lamp_only_once added: feeds take/take/look and asserts no double-pickup, no stale lamp mention. Runs ~100s because is_awaiting_input early-exit was removed (rx_queue empties before take handler completes; ticks past read_line are no-ops anyway). 22/22 tests pass. Pages rebuilt. Earlier failed attempts: 4-tuple let-destructure (let (r,inv,lt,kt) = st in ...) silently produced no take output; match st with (r,inv,lt,kt) -> ... same; Some Lamp nested constructor pattern same. Switched to fst/snd nested-pair access + Some it variable binding + inner match it dispatch -- all proven patterns from existing demos.