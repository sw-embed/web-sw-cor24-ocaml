Hand-edited examples/modules.ml under LOCAL_OVERRIDE: added inline (* ... *) comments framing each line, plus a corrective Math.add 1 2 before the deliberate-failure add 1 2. Output now reads 5/18/3/EVAL ERROR with comments making explicit that the trailing error is the namespace-isolation punchline. Updated sync-demos.sh LOCAL_OVERRIDE list (now led-toggle, guess, modules) with rationale comments. Updated src/demos.rs description for the modules entry to call out educational frame and the language's lack of try/catch. Full cargo test suite passes (17 tests, including cleanly-halts integration test running the demo against the vendored interpreter).