Re-instates the populate-demos-table step (originally step 004, aborted). Same body as the original prompt at .agentrail/steps/004-populate-demos-table/prompt.md — populate examples/ from sibling sw-cor24-ocaml/tests/ and create src/demos.rs. The new runner's Session API is compatible (new, new_interactive, tick, is_done, is_halted, output, clean_output, feed_input, is_awaiting_input) so demos.rs needs no shape changes from the original plan.