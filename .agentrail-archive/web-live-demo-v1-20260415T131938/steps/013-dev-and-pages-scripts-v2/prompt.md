Re-instates step 007. Same body as the original at .agentrail/steps/007-dev-and-pages-scripts/prompt.md: copy serve.sh + build-pages.sh from web-sw-cor24-basic adjusted for port 9735 and --public-url /web-sw-cor24-ocaml/, preserve the mkdir lock under target/.trunk-dist.lock, add pages/.nojekyll, extend .gitignore so pages/.nojekyll stays tracked while the rest of pages/ is ignored. Validation: scripts/serve.sh launches on 9735, scripts/build-pages.sh produces pages/ with index.html + wasm + css + .nojekyll.