Replaced the Rust-native p-code interpreter with cor24-emulator running the canonical pvm.s. asm/pvm.s vendored from sw-cor24-pcode; build.rs assembles it via cor24_emulator::Assembler and captures the code_ptr label. Runner loads pvm.bin at 0, ocaml.p24m at 0x010000, patches code_ptr, and ticks via emu.run_batch — pvm's own init_p24m handles multi-unit XCall/IRT. Runner: 1000 -> 269 lines. cargo test print_int_42 still passes; trunk build clean. assets/pvm.bin and code_ptr_addr.txt removed (pvm.bin now built from source; label captured at compile time).